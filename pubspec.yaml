name: aphasia_saviour
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # STORAGE
  sqflite: ^1.2.0
  
  #LOCALISATIONS
  flutter_tts:

  # WIDGETS
  flutter_slidable: "^0.5.4"
  cupertino_icons: ^0.1.2

  # UTILS
  get_it: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
  